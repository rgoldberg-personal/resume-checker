{
  "taskId": "task-11",
  "title": "Frontend: Expandable rich breakdown UI in ScoreGauge",
  "description": "Replace the current flat justification text line in ScoreGauge.tsx with an expandable per-category panel showing all five breakdown fields: reasoning, gap analysis, improvements, and both learning paths.\n\nDepends on task-10 (types updated) being complete first.\n\n--- CURRENT BEHAVIOUR ---\nEach breakdown row shows: score bar + a single <p> of text from breakdown.justifications[key] (a plain string).\n\n--- TARGET BEHAVIOUR ---\nEach breakdown row has a chevron toggle. Collapsed state shows only the score bar (same as today). Expanded state shows a structured panel with:\n- Reasoning section\n- Gap analysis section  \n- Improvements list\n- Short learning path list (labelled \"Quick wins — weeks to 2 months\")\n- Long learning path list (labelled \"Strategic path — 3-6 months\")\n\n--- IMPLEMENTATION GUIDANCE ---\n\nFile to modify: frontend/src/components/ScoreGauge.tsx\n\nDo NOT create a separate component file unless the implementation becomes unwieldy — keep it in ScoreGauge.tsx for now.\n\n1. Add React useState import (already has React imports from recharts; add useState from 'react').\n\n2. Add a state variable to track which categories are expanded:\n   ```typescript\n   const [expandedRows, setExpandedRows] = useState<Set<string>>(new Set());\n   const toggleRow = (key: string) => {\n     setExpandedRows(prev => {\n       const next = new Set(prev);\n       next.has(key) ? next.delete(key) : next.add(key);\n       return next;\n     });\n   };\n   ```\n\n3. Update the breakdown row rendering. Replace the current simple justification <p> with:\n\n```tsx\n{BREAKDOWN_ROWS.map(({ key, label, max }) => {\n  const cat = breakdown.justifications[key]; // now CategoryBreakdown, not string\n  const isExpanded = expandedRows.has(key);\n  return (\n    <div key={key} className=\"py-3\">\n      {/* Header row: label, score, chevron toggle */}\n      <button\n        onClick={() => toggleRow(key)}\n        className=\"w-full flex items-center justify-between group\"\n        aria-expanded={isExpanded}\n        aria-controls={`breakdown-${key}`}\n      >\n        <span className=\"text-sm font-medium text-gray-700\">{label}</span>\n        <div className=\"flex items-center gap-2\">\n          <span className=\"text-sm font-semibold text-gray-900 tabular-nums\">\n            {breakdown[key]}{' '}\n            <span className=\"text-gray-400 font-normal\">/ {max}</span>\n          </span>\n          {/* Chevron icon — rotates when expanded */}\n          <svg\n            className={`w-4 h-4 text-gray-400 transition-transform ${\n              isExpanded ? 'rotate-180' : ''\n            }`}\n            fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\"\n            aria-hidden=\"true\"\n          >\n            <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M19 9l-7 7-7-7\" />\n          </svg>\n        </div>\n      </button>\n\n      {/* Progress bar */}\n      <div className=\"mt-1.5 h-1.5 bg-gray-100 rounded-full overflow-hidden\">\n        <div\n          className=\"h-full bg-blue-500 rounded-full transition-all\"\n          style={{ width: `${(breakdown[key] / max) * 100}%` }}\n          aria-label={`${label}: ${breakdown[key]} of ${max}`}\n        />\n      </div>\n\n      {/* Expandable panel */}\n      {isExpanded && cat && (\n        <div id={`breakdown-${key}`} className=\"mt-3 space-y-3 pl-1\">\n\n          {/* Reasoning */}\n          {cat.reasoning && (\n            <div>\n              <p className=\"text-xs font-semibold text-gray-500 uppercase tracking-wide mb-0.5\">How scored</p>\n              <p className=\"text-xs text-gray-600 leading-snug\">{cat.reasoning}</p>\n            </div>\n          )}\n\n          {/* Gap analysis */}\n          {cat.gap_analysis && (\n            <div>\n              <p className=\"text-xs font-semibold text-gray-500 uppercase tracking-wide mb-0.5\">Gap</p>\n              <p className=\"text-xs text-gray-600 leading-snug\">{cat.gap_analysis}</p>\n            </div>\n          )}\n\n          {/* Improvements */}\n          {cat.improvements && cat.improvements.length > 0 && (\n            <div>\n              <p className=\"text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1\">Improvements</p>\n              <ul className=\"space-y-1\">\n                {cat.improvements.map((item, i) => (\n                  <li key={i} className=\"flex items-start gap-1.5\">\n                    <span className=\"mt-0.5 flex-shrink-0 w-1 h-1 rounded-full bg-blue-400\" aria-hidden=\"true\" />\n                    <span className=\"text-xs text-gray-600 leading-snug\">{item}</span>\n                  </li>\n                ))}\n              </ul>\n            </div>\n          )}\n\n          {/* Short learning path */}\n          {cat.short_learning_path && cat.short_learning_path.length > 0 && (\n            <div>\n              <p className=\"text-xs font-semibold text-amber-600 uppercase tracking-wide mb-1\">Quick wins — weeks to 2 months</p>\n              <ul className=\"space-y-1\">\n                {cat.short_learning_path.map((item, i) => (\n                  <li key={i} className=\"flex items-start gap-1.5\">\n                    <span className=\"mt-0.5 flex-shrink-0 w-1 h-1 rounded-full bg-amber-400\" aria-hidden=\"true\" />\n                    <span className=\"text-xs text-gray-600 leading-snug\">{item}</span>\n                  </li>\n                ))}\n              </ul>\n            </div>\n          )}\n\n          {/* Long learning path */}\n          {cat.long_learning_path && cat.long_learning_path.length > 0 && (\n            <div>\n              <p className=\"text-xs font-semibold text-green-700 uppercase tracking-wide mb-1\">Strategic path — 3-6 months</p>\n              <ul className=\"space-y-1\">\n                {cat.long_learning_path.map((item, i) => (\n                  <li key={i} className=\"flex items-start gap-1.5\">\n                    <span className=\"mt-0.5 flex-shrink-0 w-1 h-1 rounded-full bg-green-500\" aria-hidden=\"true\" />\n                    <span className=\"text-xs text-gray-600 leading-snug\">{item}</span>\n                  </li>\n                ))}\n              </ul>\n            </div>\n          )}\n\n        </div>\n      )}\n    </div>\n  );\n})}\n```\n\n4. Add the useState import at the top of the file:\n   import { useState } from 'react';\n\n5. Remove the old BreakdownRow type and rendering logic that referenced breakdown.justifications[key] as a string.\n\n--- ACCEPTANCE CRITERIA ---\n- Each of the four breakdown rows is clickable and toggles an expandable panel.\n- Collapsed state looks identical to the current UI (score bar only, no justification text).\n- Expanded state shows all five sections: How scored, Gap, Improvements, Quick wins, Strategic path.\n- Sections with empty arrays or empty strings are not rendered (defensive checks with &&).\n- The chevron icon rotates 180 degrees when expanded.\n- No TypeScript type errors (the justifications field is now CategoryBreakdown, not string).\n- pnpm build from frontend/ succeeds with no errors.",
  "assignment": "agent-frontend",
  "tier": ["Free"],
  "priority": "high",
  "dependencies": ["task-10"],
  "status": "completed"
}
